# Secrets — never commit these (would leak Snowflake credentials)
.env
*.env
!.env.example
# Private keys (key-pair auth)
*.p8
*.pem
*.key
rsa_key*

# Python env / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Build artifacts
*.egg-info/
dist/
build/

# OS
.DS_Store

# Un-confirmed ingest drafts (runtime data)
.ingest_drafts/

# Local sample decks / node scratch (not part of the agent)
deck/

# Scratch: files the bridge cannot delete
_to_delete/
